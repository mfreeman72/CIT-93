// === - equality operator
// !== - not equal operator
// < - less than operator
// > - greater than operator
// <= - less than or equal to operator
// >= - greater than or equal to operator

let temp = 180;

if (temp <= 32) {
  console.log("It is freezing outside!");
}

if (temp >= 100) {
  console.log("It is way too hot outside!");
}

// Challenge area

// Discount for person under age or over age

// Create age variable set to my age
// Calculate is child - if they are 7 or under
// Calculate is senior - if they are 65 or older
// Print is child value
// Print is senior value

// let age = 49
// let isChild = age <= 7
// let isSenior = age >= 65

// console.log(isChild)
// console.log(isSenior)

// -------------------------------------------

// Challenge area for video 15

let age = 49;

// If 7 or under print message about child pricing
// If 65 or older print message about senior discount

if (age <= 7) {
  console.log("Child pricing is available for ages 7 and under");
}

if (age >= 65) {
  console.log("Senior discount is available for ages 65 and older");
}
