// 1.  You can't define a variable more than once
let petName = 'Ginger'
petName = 'Spot'

console.log(petName)

// 2. There are rules related to variable names - Must start with letters, $, or _, and can't include symbols except $ and _
let 3 = 2
let result = 3 + 4

// 3. Variable names cannot be reserved keywords
let let = 12
